#############################################
# constraints.txt (optional pin overrides)
# This file is INTENTIONALLY minimal.
# Add specific pins ONLY when you must override a transitive dependency
# or freeze a security-hotfix version. Example:
# urllib3==2.1.0
# To generate fully pinned files, run:
#   pip-compile requirements.in
#   pip-compile dev-requirements.in
# and commit the resulting *.txt outputs.
#############################################
